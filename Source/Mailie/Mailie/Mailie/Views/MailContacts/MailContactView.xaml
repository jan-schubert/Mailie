<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mvvm="clr-namespace:Mailie.Mvvm;assembly=Mailie"
             xmlns:mailContacts="clr-namespace:Mailie.Views.MailContacts;assembly=Mailie"
             x:Class="Mailie.Views.MailContacts.MailContactView"
             mvvm:ViewModelLocator.ViewModelType="{x:Type mailContacts:MailContactViewModel}">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout>
                <Entry Text="{Binding Path=Item.Name}" />
                <Button Text="Save" Command="{Binding Path=SaveCommand}" />
                <Button Text="Delete" Command="{Binding Path=DeleteCommand}" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>